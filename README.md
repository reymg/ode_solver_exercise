ODE Solver exercise. This implements systems of ODE solver into Python codes.

To know if your code can run, when you run `python main.py` it should show the GUI and your airplane when you press "Launch"
